package com.example.syncwell.ui.theme

import androidx.compose.ui.graphics.Color

// Modern Fitness App Color Palette

// Primary accent colors
val Primary = Color(0xFFFF3C95) // Vibrant pink
val PrimaryVariant = Color(0xFFD1237A) // Darker pink
val Secondary = Color(0xFF6236FF) // Purple
val SecondaryVariant = Color(0xFF4E2BCF) // Darker purple

// Light theme colors (not primary focus for this app)
val Background = Color(0xFFF5F5F5) // Very light grey
val Surface = Color(0xFFFFFFFF) // White
val OnPrimary = Color(0xFFFFFFFF) // White
val OnSecondary = Color(0xFFFFFFFF) // White
val OnBackground = Color(0xFF212121) // Dark grey
val OnSurface = Color(0xFF212121) // Dark grey
val Error = Color(0xFFFF5252) // Red
val OnError = Color(0xFFFFFFFF) // White

// Dark theme colors (primary focus)
val DarkPrimary = Color(0xFFFF3C95) // Vibrant pink
val DarkPrimaryVariant = Color(0xFFD1237A) // Darker pink
val DarkBackground = Color(0xFF121214) // Very dark grey/almost black
val DarkSurface = Color(0xFF1E1E23) // Dark grey with slight purple tint
val DarkOnPrimary = Color(0xFF000000) // Black
val DarkOnSecondary = Color(0xFF000000) // Black
val DarkOnBackground = Color(0xFFF5F5F5) // Very light grey
val DarkOnSurface = Color(0xFFF5F5F5) // Very light grey

// Additional accent colors
val Success = Color(0xFF3DD598) // Green
val Warning = Color(0xFFFFB950) // Amber/Orange
val Info = Color(0xFF50BEFF) // Light blue
val Disabled = Color(0xFF6E6E7E) // Grey

// Surface colors for cards and backgrounds
val SurfaceVariant = Color(0xFF272732) // Slightly lighter dark grey
val DarkSurfaceVariant = Color(0xFF2B2B36) // Even slightly lighter dark grey

// Accent colors for visual interest
val AccentPink = Color(0xFFFF3C95) // Vibrant pink (same as primary)
val AccentPurple = Color(0xFF6236FF) // Purple
val AccentBlue = Color(0xFF50BEFF) // Light blue
val AccentGreen = Color(0xFF3DD598) // Green
val AccentOrange = Color(0xFFFFB950) // Orange

// Chart and visualization colors
val ChartPink = Color(0xFFFF3C95) // Vibrant pink
val ChartPurple = Color(0xFF6236FF) // Purple
val ChartBlue = Color(0xFF50BEFF) // Light blue
val ChartGreen = Color(0xFF3DD598) // Green
val ChartOrange = Color(0xFFFFB950) // Orange
val ChartYellow = Color(0xFFFFE580) // Yellow

// Status indicator colors
val StatusComplete = Color(0xFF3DD598) // Completion status (green)
val StatusInProgress = Color(0xFF50BEFF) // In progress status (blue)
val StatusPending = Color(0xFFFFB950) // Pending status (orange)
val StatusCancelled = Color(0xFFFF5252) // Cancelled status (red)

// UI Element colors
val CardBackground = Color(0xFF1E1E23) // Dark card background
val TabInactive = Color(0xFF6E6E7E) // Inactive tab color
val Divider = Color(0xFF2B2B36) // Divider color
val InputField = Color(0xFF272732) // Input field background

// Additional colors from the Shutter Points palette
val TealLight100 = Color(0xFFB2DFDB) // Teal 100
val TealLight50 = Color(0xFFE0F2F1) // Teal 50
val OrangeAccent300 = Color(0xFFFF9E80) // Orange 300
val OrangeDark500 = Color(0xFFFF5722) // Orange 500

// Legacy colors (kept for reference if needed)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)